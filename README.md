# Webboard (PHP-Based)

This is a simple **Webboard** application built entirely using **PHP**. All posts are stored in `.txt` files, so no database is required. It's lightweight and easy to set up.

---

## Features

- Post messages with a title and content.
- View all posts on the main page.
- No database needed – everything is saved in text files.

---

## Requirements

- PHP 7.x or higher
- A web server (or use PHP's built-in server)

---

## Installation

1. **Clone the Repository**:
   ```bash
   git clone https://github.com/Bobby9326/Webboard.git
   cd Webboard
